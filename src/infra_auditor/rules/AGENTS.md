# Rule Instructions

Rules turn collected evidence into deterministic findings. They must not call AWS, PostgreSQL, LLMs, or MCP tools directly.

Findings must cite evidence references and keep observed facts separate from policy thresholds, vendor/default context, and recommendations.
