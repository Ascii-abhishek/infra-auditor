# MCP Layer

No MCP server exists in V0.1.

Future MCP tools should query the audit system, not production databases directly. A generic `execute_sql(sql)` or `run_query(sql)` tool is explicitly out of bounds.
