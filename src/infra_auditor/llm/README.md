# LLM Layer

No LLM integration exists in V0.1.

Future LLM analysts may receive normalized evidence, deterministic findings, sanitized query metadata, and configuration context. They must never receive credentials, secret values, application table rows, raw FDW secrets, or arbitrary live database access.
